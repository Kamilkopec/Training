# please create an empty file (manualy) and nami it requests.py. make sure the file has
# that name exacly
# then just past the following code in the file:


# the import statement is the same as file name
# the program will import the file name where there is no get method defined
#  Script names load in the current namespace. They are actually stored in the __name__  variable.
# You could try to print that variable out and you would see that it prints your script name.
# Therefore you should not name your scripts under library names.



#import requests

# # r = requests.get("http://www.pythonhow.com")
# r = requests.get('https://www.pythonhow.com')
#
# print(r.text[:100])
#
# # we are